{% extends '::base.html.twig' %}

{% block content -%}
    <h1>{{ 'tj.user.notification.settings.creation' | trans }}</h1>

    {{ form(form) }}

        {#<ul class="record_actions">
    <li>
        <a href="{{ path('notificationsettings') }}">
            Back to the list
        </a>
    </li>
</ul>#}
{% endblock %}

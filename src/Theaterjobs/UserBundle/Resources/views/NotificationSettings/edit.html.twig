{% extends '::base.html.twig' %}

{% block content -%}
    <h1>{{ 'tj.user.notification.settings.edit' | trans }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    {#<li>
        <a href="{{ path('notificationsettings') }}">
            Back to the list
        </a>
    </li>#}
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
